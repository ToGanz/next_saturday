<% @user ||= current_user %>
<div class="stats">
  <a href="<%= user_path(@user) %>">
    <strong id="following" class="stat">
      <%= @user.attended_events.count %>
    </strong>
    attended events
  </a>
</div>